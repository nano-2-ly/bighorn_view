<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bighorn_view">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BIGHORN_VIEW">
        <activity android:name=".DataDetailActivity"></activity>
        <activity android:name=".GraphActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".NoticeDetailActivity" />
        <activity android:name=".QuestionDetailActivity" />
        <activity android:name=".CreateQuestionActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NoticeRvAdapter" />
        <activity android:name=".DataViewActivity" />
        <activity android:name=".NoticeActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>